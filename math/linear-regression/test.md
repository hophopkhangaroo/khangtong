---
layout: math
title: Test
usemathjax: true
description:
---

This is a test:

```r
library(car)
fit <- lm(Y ~ X, data=df)
binom.confint(4, 6, method="ac")
tlmgr_install(
  pkgs = character(),
  usermode = FALSE,
  path = !usermode && os != "windows"
dantal = read_table2("Dental.dat",
	col = FALSE, skip=27)
colnames(dental) = c("ID")
```